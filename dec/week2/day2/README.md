React
=========
-> it is a library(predefine codes, predefine functionality)
-> in one page we can write html,css,js


node_modules
-> alll library files inside it

pulic
->everyone can see it directly

src
->store all the files in it

.gitignore
->when we push it into the git, the files which are present inside it are not push to git, they are ignored
-> basically secret files present in it

eslint.config.js
->config(complete information) -> store all the execution url
->eslint -> language

index.html
->browser understandable file

.jsx file
->javascript xml 
-> it is a file where we can write both html and javascript
->if we want  to write any javascript code we have to use {}
->eg: {console.log("hii")}
->it is a component
->component has two part
    1. class based
    2. function based
->it is by default function based
-> it is by default html page
-> if we create a jsx file , the first character is capital (eg: Home.jsx)


how to know hook
=================
->if "use" keyword is used there then you can know that it is a hook


context object is a type of object where if any child want to call it from the parent by using hook it can be accessed



work of functional component
=============================
->perform a specific task
->reuse