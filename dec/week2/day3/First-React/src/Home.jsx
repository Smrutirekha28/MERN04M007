const Home = function(){ 
return(// it return a html code
        <div>
            This is home page
        </div>
    )
}
export default Home; // this line is used to (home is used in anaother file(app.jsx))