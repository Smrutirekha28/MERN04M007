->let n="1";
console.log(1+3-1+ +n+4+"5");//here +n convert type-casting and that +n convert "1" into 1

-> explain for-in, for-of

-> where and how to use string object
    let objStr = new String()//it means when the variable objStr is stored , it stored as String object 

-> interpolation, template literal

-> write a program to count the number of vowels in a string
let str = "enter anything";
let vowel = ["a","e","i","o","u","A","E","I","O","U"];
let vowelCount=0;
for(let v of str){
    if(vowel.includes(v)){
        vowelCount++;
        
    }
}console.log(vowelCount);


-> use of "of" method in array
    ->store the value in a array using comma separated


-> flat method
    ->flat all the nested array in single array
-> join method
    ->join the value and store in string
-> fill method
    ->to store the value default
 


-> IIF(immdiate invoke(calling) function)
-> higher order function
-> for each method
 